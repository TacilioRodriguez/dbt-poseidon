version: 2

models:
  - name: stg_users
    description: "Tabela staging de usuários brutos"
    columns:
      - name: user_id
        description: "Identificador único do usuário"
        tests:
          - not_null
          - unique
