[sqlfluff]
dialect = databricks
templater = dbt

[sqlfluff:templater:dbt]
# O projeto dbt está na raiz do repositório
project_dir = $GITHUB_WORKSPACE
profiles_dir = /tmp/dbt_profiles

[sqlfluff:rules]
capitalisation_policy = lower
