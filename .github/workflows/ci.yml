name: CI - dbt Project

on:
  pull_request:
    branches:
      - main

jobs:
  dbt-ci:
    name: dbt CI Pipeline
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout do código
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup do Python
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3️⃣ Instalar dependências
      - name: Install Python dependencies
        run: |
          echo "🐍 Installing dbt and SQLFluff"
          python -m pip install --upgrade pip
          pip install dbt-core dbt-databricks
          pip install sqlfluff sqlfluff-templater-dbt

      # 4️⃣ Criar profiles.yml fake (somente para CI)
      - name: Create fake dbt profiles
        run: |
          echo "🧪 Creating fake dbt profiles for CI"
          mkdir -p /tmp/dbt_profiles
          cat <<EOF > /tmp/dbt_profiles/profiles.yml
          dbt_poseidon:
            target: ci
            outputs:
              ci:
                type: databricks
                host: fake
                http_path: fake
                token: fake
                catalog: fake
                schema: fake
                threads: 1
          EOF

      # 5️⃣ SQLFluff lint (dbt + Databricks)
      - name: SQLFluff lint (dbt + Databricks)
        working-directory: .
        run: |
          echo "🔍 Running SQLFluff lint from repo root"
          echo "📄 Config: ci/sqlfluff/.sqlfluff"
          echo "📂 Models path: models/"
          echo "📁 Current dir: $(pwd)"
          echo ""

          sqlfluff lint models \
            --config ci/sqlfluff/.sqlfluff \
            --templater-dbt-project-dir . \
            --verbose \
            --format github-annotation \
            || (
              echo ""
              echo "❌ SQLFluff validation FAILED"
              echo "👉 One or more dbt models failed linting."
              echo "👉 See logs above for file names and line numbers."
              exit 1
            )

      # 6️⃣ dbt debug — valida estrutura (sem conexão real)
      - name: dbt debug (structure validation)
        run: |
          echo "🧪 Running dbt debug"
          dbt debug \
            --profiles-dir /tmp/dbt_profiles \
            || (
              echo ""
              echo "❌ dbt debug FAILED"
              echo "👉 Check dbt_project.yml structure."
              exit 1
            )
